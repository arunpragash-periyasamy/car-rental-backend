# Documentation tickets — car-rental-backend

Generated by github-repo-documenter on 2026-09-06. Tracks what was found, what was written, and what
was verified before commit.

| ID | Ticket | Status | Notes |
|----|--------|--------|-------|
| T1 | README: title, status, overview, tech stack, features | Verified | Stack confirmed against `pom.xml` (Spring Boot 3.3.1, Java 21, MySQL, JWT via jjwt); features confirmed against each controller/service |
| T2 | README: getting started + usage | Verified | Property list taken from the existing `Readme.md`, cross-checked against `@Value` usages (`app.jwtSecret`, `app.url`) in `JwtService`/`CarService`/`BookingService`; run commands confirmed against `pom.xml` (Maven, no Gradle) and `Dockerfile` |
| T3 | ARCHITECTURE: system overview + component diagram | Verified | Components match actual controller/service/config classes; JWT filter chain confirmed in `SecurityConfig` + `JwtFilter` |
| T4 | ARCHITECTURE: DB schema diagram | Verified | Built from the 12 `@Entity` classes in `model/table/`, cross-checked column-for-column against the real schema dump in `output.sql`; `Document`/`Place` confirmed unused (no repository exists for either) |
| T5 | ARCHITECTURE: folder-by-folder breakdown | Verified | Cross-checked against actual `src/main/java` tree via `find` |
| T6 | ARCHITECTURE: booking sequence diagram | Verified | Traced step-by-step against `BookingService.createBooking` source, including the hardcoded `SUCCESS` transaction status |
| T7 | README/ARCHITECTURE: Known Issues / security findings | Verified | Each item confirmed by reading the actual source: hardcoded Gmail credentials in `MailConfig.java`, plaintext card storage in `BookingService`/`CardDetails`, IDOR in `CarController.updateCar` and `UserController.updateUser`, hardcoded `USER` authority in `UserPrincipal`, leftover PII in a `UserService.java` comment, wide-open CORS in `WebConfig` + `@CrossOrigin`, a leading-slash inconsistency in `SecurityConfig`'s permit-all matchers, committed build artifacts (50MB jar, `output.sql`, `spring.log`), and absence of tests/CI |
| T8 | Rename `Readme.md` → `README.md` | Verified | Two-step `git mv` used to force the case change through git on a case-insensitive filesystem |

## Summary

Wrote README.md, ARCHITECTURE.md, and this TICKETS.md for a real Spring Boot backend (not a build artifact or frontend). Confirmed its route surface (`/api/auth`, `/api/cars`, `/api/bookings`, `/api/users/expired`) matches what the separately-documented `Car-Rental-Frontend` calls, confirming this repo is the actual backend implementation for that frontend. Found several genuine security issues during the scan (hardcoded email credentials committed to a public repo, plaintext card number/CVV storage, two IDOR-style missing ownership checks, no real role enforcement) and documented them honestly in both files rather than smoothing them over. All tickets verified on first pass; pushed to branch `docs/update-readme-architecture`.
